

/my-project
│
├── docker-compose.yml
│
├── shared ( package format that can be installed in docker conmatiner as per need , it can be a python or js package has react components )
│   ├── frontend
│   │   ├── components
│   │   ├── hooks
│   │   ├── utils
│   │   └── package.json
│   │
│   └── backend ( all are python packages can be installed using pip )
│       ├── hash-utils
|       └── redis-helper-kit
|       └── nongo-helper-kit
│       └── requirements.txt
|
├── frontend
│   ├── main-website
│   │   ├── public
│   │   ├── src
│   │   ├── package.json
│   │   ├── Dockerfile
│   │   └── .dockerignore
│   │
│   ├── chat-app
│   │   ├── public
│   │   ├── src
│   │   ├── package.json
│   │   ├── Dockerfile
│   │   └── .dockerignore
│   │
│   ├── tinyurl
│   │   ├── public
│   │   ├── src
│   │   ├── package.json
│   │   ├── Dockerfile
│   │   └── .dockerignore
│   │
│   └── static-website
│       ├── public
│       ├── src
│       ├── package.json
│       ├── Dockerfile
│       └── .dockerignore
│
└── backend
    ├── main-website
    │   ├── app.py
    │   ├── requirements.txt
    │   ├── Dockerfile
    │   └── .dockerignore
    │
    ├── chat-app
    │   ├── app.py
    │   ├── requirements.txt
    │   ├── Dockerfile
    │   └── .dockerignore
    │
    ├── tinyurl
    │   ├── app.py
    │   ├── requirements.txt
    │   ├── Dockerfile
    │   └── .dockerignore
    │
    └── static-website
        ├── app.py
        ├── requirements.txt
        ├── Dockerfile
        └── .dockerignore
|
└── redis ( docker container )
└── mongo ( docker conatiner )
└── nginx (running bare metal)
└── certbot-certifcate 
└── mongo-data-insertion
└── data-files (json format)



the package scrapping ---
- deployment -
  - bulk data insertion
  - read-data-json




--------------------------------------------------------------------
the front-end scrapping 

- front-end shared components
  - base  (done)
  - header bar  (done )
  - button  (done)
  - contact form 
  - card  (done)
  - more (done)
  - markdown container
  - qr code generater
  - copy url 
  - image container
  - article desc
  - footer
  - social media container
  - more page number




--------------------------------------------------------------------------

Database design 

database - 

mongodb - 
  - article (db)
    - project (collection)
    - tech (collection)
    - life (collection)
  
  - message-data (db)
    - messages (collection)







artcile storage format - 

  {
  article_id (unique comes from redis helper)
  article_title  (card)
  article_card_Image (card)
  article_short_description (card)
  explore_page_link (card)
  article_data [
  heading, img_url, description , markdown_data
  heading, img_url, descriptionn , markdown_data
  heading , img_url ,description , markdown_data
  ]
  social_link {
    github_link 
    gitlab_link 
    medium_link

  }
  demo-link  

  }




### Imp

 - if a card is not there make the space for card (exact space ) , so rendering is unique
 - make the logic for displaying only 9 cards per page and the next goes other page
 - The unique hash comes from unique hash maker always 
 - unique-hash-stored  = unique-hash-dict (in redis hash map) for the used hashes 
 - unique-hash-generated = unique-has-set ( in redis hash set) to not generate duplicate hashses 



--------------------------------------------------------------------------------------------

changes as per data fetching and react app 

pass the data from bakend as a api call /data 

 - how the mutiple database and collections can be handled 
 - 



in data all the data can be access by loops and keys
- start the flask and the react sevre on 5000 and 3000 port 
- example 

    in flask code -- 
    from flask import Flask, jsonify
    from flask_cors import CORS

    app = Flask(__name__)
    CORS(app)  # This will allow cross-origin requests from any domain

      @app.route("/exp")
      def experience():
          data = {
              "message": "Welcome to the experience page!",
              "number": 42
          }
          return jsonify(data)
        
      
    in front end code -- 

    function Experience() {
      const [data, setData] = useState(null); // State to store fetched data
      const [loading, setLoading] = useState(true); // State to show loading status

      useEffect(() => {
        // Fetch data from the Flask API
        fetch("http://localhost:5000/exp") // Adjust URL if necessary
          .then((response) => response.json())
          .then((data) => {
            setData(data); // Store the data in the state
            setLoading(false); // Set loading to false after data is fetched
          })
          .catch((error) => console.error("Error fetching data:", error));
      }, []);

      if (loading) return <p>Loading...</p>;

      return (
        <div>
          <h1>Message: {data.message}</h1>
          <p>Number: {data.number}</p>
        </div>
      );
    }

    export default Experience;
          
  




flask render will not be used now 
flask will only retun the json data and process it 
state Management in react will be used to manipute the dom 
- The card component can hold max of 9 crads and then next page 
- the card componet should can also display 3 comoponnets from one section then different card componete with different section 
- collection can be passed as parameter in card and then loop can be run as per times to display the data 
- 

































































### Summary of Shared Resources

1. **Code Reuse**: By having a `shared` directory, you can avoid duplicating code across your projects. This promotes maintainability and consistency.
  
2. **Shared Frontend Components**: Components like buttons, form fields, and common UI patterns can be reused across the main website and all individual project frontends.

3. **Shared Backend Logic**: Utility functions, models, and service logic that are common across different backends can be placed in the shared backend directory.

4. **Modular Deployment**: Each project can still be deployed independently, but they can leverage shared resources, allowing you to manage updates and changes more easily.

5. **Docker Management**: The Docker setup ensures that all services can build and access shared components seamlessly.

With this architecture, you maintain a clear separation of concerns while maximizing code reuse and keeping your project organized.









