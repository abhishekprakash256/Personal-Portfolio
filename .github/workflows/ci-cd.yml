name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check out the code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '16'
    
    - name: Test the command
      run : apt-get update
        

    - name: Deploy
      run: |
        echo "Deploying to production"
        # Add your deployment commands here
        # For example, you might use rsync, scp, or a specific deployment script.
